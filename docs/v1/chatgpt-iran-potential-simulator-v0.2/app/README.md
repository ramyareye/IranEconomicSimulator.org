# App (placeholder)

This repo ships without a UI by design (you said UI isn't important now).

Suggested stack:
- Web: Next.js or Vite + React
- Mobile: React Native (Expo)
- Data: fetch JSON scenarios/parameters from GitHub raw, with caching

Minimal MVP pages:
- `/` landing: pick scenario -> show headline metrics + 1 chart
- `/scenario/:id` deep link for sharing on Twitter/HN
- `/methodology` and `/sources`
